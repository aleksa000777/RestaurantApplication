<h1>Show page parties</h1>


    <!-- Name: <%#= @party.name %><br> -->
    number of people: <%= @party.nmofpeople %><br>


    pay_status: <%= @party.pay_status %><br>
    <!-- bill: <%#=@total %><br> -->

    Mood: <%= @party.mood %><br>
    table name: <%= @party.table.name %><br>
    location: <%= @party.table.location %><br>

<%# binding.pry %>
    Server: <%# @party.server.name %> <br>

    orders: <br>
    <ul>
      <% @total = 0 %>
    <% @orders.each do |order| %>

    <% if order.party==nil %>
    <% else %>

    <% if @party.id == order.party.id %>

    <li>
    <%= order.quantity %>  *  <%= order.food.name %> :<%= order.food.price%>$  total: <%= order.food.price*order.quantity %> $
    <% @total+=order.food.price*order.quantity %>
    <% @party.bill = @total%>
</li>

        <% end %>

        <% end %>

        <% end %>
</ul>
TOTAL: $<%= @total %><br>

    <%= link_to "new order", new_party_order_path(@party.id) %>
<h4> <%#= link_to "New order", new_order_path(@party.id) %>  </h4>


    <!-- create_table :parties do |t|
      t.string :name
      t.integer :nmofpeople
      t.boolean :pay_status
      t.decimal :bill
      t.string :mood
      t.integer :order_id
      t.integer :table_id -->

    <div class="nav">
    <%= button_to 'Edit party', edit_party_path(@party), method: :get, class: "btn btn-warning" %>
    <!-- <a class="btn btn-primary" href="/voyages" role="button">Back to voyages</a> -->
    <a class="btn btn-primary" href="/parties" role="button">Back to parties</a>

  </div>
